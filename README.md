# Inkswarm LifeLab

> WebGPU particle-life / ink-trails lifeform lab.  
> **Status:** D-0001 scaffold. (Full docs arrive in D-0009.)

## Quickstart (local)
```bash
npm install
npm run dev
```

## Notes
- WebGPU is required for the MVP. If unsupported, the app will hard-fail with a polished explanation screen (no fallback sim).
- GitHub Pages build-to-`/docs` on `main` is planned for D-0010.
- License: **no license for now** (intentionally).
